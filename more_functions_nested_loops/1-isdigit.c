#include "main.h"
/**
 * _isdigit - Main entry
 * @c: variable
 * Return: checking digits, return 0 or 1
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
	return (0);
}
